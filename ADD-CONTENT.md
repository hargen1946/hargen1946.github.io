# 新しいゲーム・パズル・アプリを追加する手順

ホームページは `catalog.json` の内容を読み込み、作品カードを自動で作ります。
新しい作品を追加するときは、次の2段階で作業します。

## 1. 作品のファイルを追加する

作品ごとにフォルダーを1つ作る方法がおすすめです。

例：新しい「神経衰弱」を追加する場合

```text
memory-game/
  index.html
  style.css
  app.js
```

この場合、公開後のアドレスは `/memory-game/` になります。

共通の文字サイズ・音の設定を使う場合は、作品の `index.html` の `<head>` に次の2行を入れます。

```html
<link rel="stylesheet" href="/assets/base.css">
<script src="/assets/ui.js"></script>
```

トップページへ戻るリンクも付けてください。

```html
<a class="back-link" href="/">← ゲームひろばへ もどる</a>
```

## 2. `catalog.json` に作品を登録する

`catalog.json` の最後の作品の後ろにカンマを付け、次のような情報を追加します。

```json
{
  "title": "神経衰弱",
  "category": "ゲーム",
  "icon": "🃏",
  "description": "同じ絵のカードを2枚ずつ見つけるゲームです。",
  "path": "/memory-game/",
  "tags": ["カード", "記憶"]
}
```

- `title`：ホームに表示する名前
- `category`：`ゲーム`、`パズル`、`便利アプリ` など
- `icon`：カードに表示する絵文字
- `description`：短い説明
- `path`：作品を開くアドレス
- `tags`：検索に使う短い言葉

GitHubへ保存すると、ホームページに新しいカードと種類ボタンが自動で追加されます。

## 注意点

- JSONでは、各項目の最後にカンマが必要です。ただし最後の項目の後ろには付けません。
- フォルダー名には半角英数字とハイフンを使うと安全です。
- 公開後は、作品カード、作品ページ、ホームへ戻るリンクをスマートフォンで確認します。
